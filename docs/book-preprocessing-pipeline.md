# Book preprocessing pipeline

The canonical preprocessing documentation moved to
`docs/domains/book-preprocessing.md`.

This file remains only as a compatibility pointer for older references. Do not
duplicate pipeline details here. Update the domain document when changing
preprocessing responsibilities, stage contracts, fingerprint rules, persistence
rules, or test contracts.
